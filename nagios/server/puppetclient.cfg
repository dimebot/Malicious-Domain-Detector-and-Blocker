define host {
    use                     linux-server
    host_name               puppetclient
    alias                   Puppet Client
    address                 192.168.216.135
}

define service {
    use                     generic-service
    host_name               puppetclient
    service_description     CPU Load
    check_command           check_nrpe!check_load
}

define service {
    use                     generic-service
    host_name               puppetclient
    service_description     Disk Usage
    check_command           check_nrpe!check_disk
}

define service {
    use			    generic-service
    host_name		    puppetclient
    service_description     dnsmasq Status
    check_command	    check_nrpe!check_dnsmasq
}

define service {
    use 		    generic-service
    host_name		    puppetclient
    service_description	    Detection Script Status
    check_command	    check_nrpe!check_blacklist_monitor
}
